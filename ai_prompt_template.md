# AI Prompt Template

你现在是我的服务器部署与运维顾问。

下面是我通过只读脚本生成的服务器现状报告。请你严格基于报告内容分析，不要假设报告中没有出现的信息。

我的目标是：在这台服务器上新增部署项目、排查问题，或判断当前服务器是否具备某些部署条件，同时不影响现有项目。

请你帮我完成以下分析：

1. 当前服务器上已经有哪些疑似项目？
2. 每个项目大概率是通过什么方式运行的？
   - PM2
   - Docker
   - systemd
   - Nginx 反向代理
   - 其他方式
3. 当前有哪些端口已经被占用？
4. 是否存在 3000、3001、3010、5000、5173、8000、8080、80、443 等常见端口冲突？
5. 当前服务器是否具备部署 Node.js 项目的基础环境？
6. 当前服务器是否具备部署 Docker 项目的基础环境？
7. 当前 Nginx 是否已经安装和运行？
8. 如果我要新增一个项目，请你推荐：
   - 项目目录
   - 内部运行端口
   - 进程管理方式
   - 是否需要 Nginx
   - 是否需要新域名或子路径
9. 请列出新增项目部署前的风险点。
10. 请给出下一步最小安全检查命令。

约束：

- 不要建议删除文件。
- 不要建议停止已有服务。
- 不要修改已有 Nginx 配置，除非先备份。
- 不要读取或输出 `.env` 内容。
- 如果信息不足，请明确说“报告中没有证据”，不要猜。
- 如果报告中出现 permission denied，请把它视为“当前用户权限不足”，不要视为资源不存在。
- 所有修改类操作都需要先给我确认。

下面是服务器现状报告：

```markdown
在这里粘贴 reports/server_context_latest.md 的完整内容
```
